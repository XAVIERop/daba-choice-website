# Optional: use a YouTube iframe as hero background (often unreliable; default is HTML5 MP4).
# VITE_HERO_USE_YOUTUBE=1

# Plattr Supabase (Daba Choice uses Plattr only — Food Club not involved)
VITE_SUPABASE_URL=https://yamjjiwifuiuhxzlnqzx.supabase.co
VITE_SUPABASE_ANON_KEY=your-plattr-anon-key
# Run migration 20260320000001_create_white_label_order.sql first
# Run 20250825190061_manual_order_system.sql if manual-orders user doesn't exist
